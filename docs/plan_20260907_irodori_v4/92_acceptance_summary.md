# v4.1移行の受入要約

状態: done。2026-09-08に実装と独立検証を完了し、利用者が最終承認した。

- REQ-01～05: 固定モデル/依存、tokenizer互換、CPU/CUDAの6 graph parity、条件/CFG/duration、製品Serviceの48kHz mono PCM16生成を確認。
- REQ-06～07: 新規設定のv4.1既定、保存済みv3/任意パス/参照音声保持、BGM共存、取消/join/Close、CPU/auto/CUDAの連続実行を確認。
- REQ-08: 正式170行の件数・条件・生成成功とv4の60秒期限を確認。v3との資源同等性は利用者判断で採用条件から除外。p95比1.93365やVRAM欠測は診断として保持し、旧測定失敗を書き換えていない。
- REQ-09: 正式60 WAVの形式/hashを確認し、利用者が両モデルとも聞き取れるアナウンス品質として許容。
- REQ-10: core E2E、Go/race、frontend/Wails build、文書の独立検証を完了。利用者がアプリを起動して一通り操作し、問題なしと最終承認。

最終利用者回答:

> アプリの起動と動作確認を行いました。一通り触ってみましたが特に問題は無いと判断しました。この内容で承認します。

最終独立完了確認の対象snapshot: `21cc197b46b87b936d9daf9a1a11c875025ce760`。完了記録までのsnapshot: `c832229f8a7e421ee68be02f4e03abad883ba040`。個々の実UI操作ログや利用者が実行したEXEのhashを追加確認したという意味ではない。

## 成果物の管理

リポジトリには実装・テスト・再現手順・要件と本要約を残す。`/evidence/` と `/docs/**/evidence/` はローカルの検証成果物であり、ログ・音声・計測JSON/CSV・作業レポートを含め追跡しない。既存文書のevidence参照は検証を実施したローカル環境の履歴参照で、新しいcloneには含まれない。

ライセンス文書は `docs/licenses/irodori/` で追跡する。元のローカル成果物は削除せず保存する。今回の整理は管理対象の変更であり、完了した製品の動作・採用判断を変更しない。
